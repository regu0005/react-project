Execute the folow command to be ready to use this repo:

    yarn install -y

    yarn add react-router-dom

    npm install react-datepicker

    npx vite build

    yarn dev

To Build the folder DIST execute before made a PUSH into the branch:

    npx vite build

    sudo netlify dev —live 